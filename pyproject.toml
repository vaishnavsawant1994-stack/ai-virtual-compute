[project]
name = "ai-virtual-compute"
version = "0.0.0"
description = "Secure capability-based virtual compute control plane"
requires-python = ">=3.11"
license = { text = "Proprietary" }

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
