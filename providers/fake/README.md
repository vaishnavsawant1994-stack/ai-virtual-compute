# FakeDeviceProvider (Stage 1)

Reserved for the deterministic Stage-1 provider. It will support injected delay, timeout, failure, stale observation, partial success, and unknown outcome so lifecycle and recovery behavior can be tested before real virtualization is introduced.
