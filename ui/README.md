# Device Center UI

The Device Center is a Stage-11 management interface for devices, sessions, live screens, human takeover, resources, cost, approvals, security alerts, snapshots, destruction, and audit history. UI state is never authoritative security state.
