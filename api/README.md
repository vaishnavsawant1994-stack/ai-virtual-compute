# API Implementation

Stage 1 will implement the REST foundation from `docs/api/api-v1.md` and `contracts/`. Public API handlers authenticate and validate requests, then delegate authority to runtime services; they do not embed provider logic.
