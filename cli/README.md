# CLI

The CLI is a Stage-10 client of the authenticated API. It is not an administrative bypass and must operate under the same principal, policy, lease, quota, budget, and audit rules as other clients.
